#include <iostream>
using namespace std;

/* 
🔢 BASIC DATA TYPE KEYWORDS
---------------------------
int     - Stores an integer (4 bytes typical)
float   - Stores a floating point number (4 bytes)
double  - Stores a double-precision float (8 bytes)
char    - Stores a single character (1 byte)
bool    - Stores true/false (1 byte)
void    - Represents no return type (used in functions)
string  - Not a keyword, from <string>, stores text

🧬 TYPE MODIFIERS
---------------------------
long      - Extends size of int or double
short     - Shorter int (usually 2 bytes)
unsigned  - Positive-only values
signed    - Allows positive & negative (default)
auto      - Automatically detects type

🧰 STORAGE CLASS SPECIFIERS
---------------------------
const     - Makes variable constant (read-only)
static    - Keeps value between function calls
extern    - Declares a global var from another file
register  - Store var in a CPU register (rarely used)
volatile  - Prevents compiler from optimizing the var

📌 CONTROL & STRUCTURE
---------------------------
return, break, continue
if, else, switch, case
for, while, do
struct, class
public, private, protected
new, delete, this
namespace, using

🧯 EXCEPTIONS & TEMPLATES
---------------------------
try, catch, throw
template, typename
enum, typedef

🧠 MODERN C++ KEYWORDS
---------------------------
nullptr       - Replaces NULL
constexpr     - Compile-time evaluation
decltype      - Get type of expression
override/final- Control virtual function inheritance
noexcept      - Function won't throw
static_assert - Compile-time assert
*/

int main() {
  return 0;
}